# Start: Application Bootstrap

Execute the startup sequence for this application:

1. **Check environment** — verify dependencies are installed and env vars are set.
2. **Run startup command**:
   - [Replace with your actual startup command, e.g.:]
   - Python: `uv run app.py` or `python main.py`
   - Node: `npm run dev` or `bun start`
   - Other: `[your command here]`
3. **Verify** the application is running correctly.
4. **Report** startup status and any issues.

If startup fails, diagnose the error and suggest fixes.
