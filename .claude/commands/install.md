# Install & Prime

Install all project dependencies and prime the agent with codebase knowledge.

## Install Dependencies

```bash
# Install main application dependencies
cd app && npm install && cd ..

# Install AI Developer Workflow dependencies
cd adws && npm install && cd ..
```

## Prime Agent

Read and execute `.claude/commands/prime.md` to understand the codebase.
